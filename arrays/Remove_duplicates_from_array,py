class Solution(object):
    def removeDuplicates(self,nums):
        num = []
        for i in nums:
            if i not in num:
                num.append(i)
           
        for i in range(len(num)):
            nums[i] = num[i]

        return len(num)
    
